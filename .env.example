# SHA-256 hash (hex) of the admin password for /admin.
# Generate: node -e "console.log(require('crypto').createHash('sha256').update(process.argv[1]).digest('hex'))" 'dein-passwort'
NEXT_PUBLIC_ADMIN_PASSWORD_HASH=
